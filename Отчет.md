# Отчет по практической работе

## Разработка CLI-утилиты MovieTracker CLI с использованием ИИ-инструментов

**Проект:** MovieTracker CLI  
**Язык разработки:** Python  
**Формат приложения:** CLI, Command Line Interface  
**Сравниваемые версии:** Claude Edition (Sonnet 4.6) и Codex Edition (GPT 5.5)

---

## 1. Введение

Целью практической работы была разработка CLI-утилиты на Python с использованием двух разных ИИ-инструментов и последующее сравнение полученных результатов.

В качестве темы был выбран проект **MovieTracker CLI** — консольная утилита для поиска, отслеживания и анализа кинофильмов и сериалов. Приложение позволяет искать контент, вести персональный список просмотра, выставлять оценки и получать рекомендации.

Техническое задание изначально ориентировалось на API **The Movie Database (TMDB)**. По итогам работы обе модели адаптировали реализацию под **Kinopoisk API Unofficial** — отечественный аналог без необходимости VPN или прокси. Это решение обеих моделей стало первым заметным расхождением с исходным ТЗ, однако сделало приложение доступным без дополнительных настроек сети.

Дополнительно работа позволила сравнить не только итоговый код, но и сам процесс разработки с помощью ИИ: скорость получения результата, количество итераций, расход токенов, архитектурные решения и тестовое покрытие.

---

## 2. Цель и задачи работы

### Цель

Разработать две независимые версии CLI-приложения для отслеживания фильмов и сериалов и сравнить эффективность ИИ-инструментов, использованных при разработке.

### Основные задачи

- составить техническое задание для CLI-утилиты;
- реализовать две версии проекта независимо друг от друга;
- использовать внешний REST API для получения данных о фильмах;
- реализовать локальное хранение watchlist, оценок и заметок;
- добавить кэширование запросов к API;
- реализовать обработку ошибок;
- подготовить тесты;
- сравнить две реализации по техническим и процессным критериям;
- сделать вывод о том, какой инструмент оказался эффективнее.

---

## 3. Техническое задание

Согласно ТЗ, приложение **MovieTracker CLI** должно быть консольной программой на Python, которая позволяет искать, отслеживать и анализировать фильмы и сериалы через внешний REST API.

### Обязательные команды CLI

| Команда | Назначение |
|---|---|
| `search` | Поиск фильмов и сериалов по названию |
| `details` | Подробная информация: описание, актёры, рецензии, похожие |
| `add-watchlist` | Добавить фильм в список просмотра |
| `list-watchlist` | Просмотреть список с фильтрацией и статистикой |
| `rate` | Выставить оценку и добавить отзыв |
| `recommend` | Получить рекомендации по фильму или жанру |
| `popular` | Топ популярных фильмов и сериалов |
| `trending` | Трендовый контент за день или неделю |
| `auth` | Управление API-ключом |
| `cache` | Управление локальным кэшем |

### Требования к данным

- внешний REST API для поиска и получения информации о фильмах;
- локальное хранилище watchlist в JSON-формате;
- кэширование ответов API с дифференцированным TTL;
- форматы вывода: таблица, JSON, CSV.

### Нефункциональные требования

- Python 3.11+;
- читаемый вывод в консоли (Rich);
- соответствие PEP 8;
- обработка сетевых ошибок и ошибок API;
- покрытие кода тестами не менее 80%;
- совместимость с macOS, Linux и Windows.

---

## 4. Методология сравнения

Разработка велась в двух отдельных папках:

- `Claude/` — версия, полученная при работе с Claude Sonnet 4.6;
- `Codex/` — альтернативная версия, полученная при работе с GPT 5.5.

Обе версии решали одну и ту же задачу и ориентировались на одно техническое задание. При сравнении учитывались следующие критерии:

- полнота выполнения ТЗ;
- количество запросов и итераций до рабочего результата;
- расход токенов и стоимость;
- архитектура и качество кода;
- количество и структура тестов;
- удобство запуска и документации;
- готовность к распространению.

Для сравнения использовались итоговый код, история диалогов и статистика usage по моделям. По usage-данным оценивались количество запросов, токены и стоимость. Количество итераций оценивалось по сложности пути до рабочего результата.

---

## 5. Реализация Claude Edition

Версия `Claude` представляет собой CLI-приложение с акцентом на строгую типизацию, слоистую архитектуру и максимальное тестовое покрытие.

### Используемые источники данных

- **Kinopoisk API Unofficial** — поиск фильмов, детали, похожие, популярное, тренды;
- **Локальное хранилище** — watchlist, оценки, заметки, теги (TinyDB + JSON).

### Технологический стек

| Компонент | Технология | Версия |
|---|---|---|
| CLI-фреймворк | Typer | 0.12+ |
| HTTP-клиент | httpx (async) + anyio | 0.27+ |
| Вывод в терминал | Rich | 13+ |
| Кэширование | diskcache | 5.6+ |
| Локальная БД | TinyDB | 4.8+ |
| Конфигурация | dynaconf | 3.2+ |
| Логирование | loguru | 0.7+ |
| Сборка | setuptools | 68+ |

### Основные файлы проекта

| Файл / Модуль | Назначение |
|---|---|
| `src/movie_tracker/main.py` | Точка входа, Typer app |
| `src/movie_tracker/cli/` | Команды: search, details, watchlist, auth, actions |
| `src/movie_tracker/api/` | HTTP-клиент Kinopoisk: search, details, auth |
| `src/movie_tracker/storage/` | TinyDB watchlist + хранение токена |
| `src/movie_tracker/cache/` | diskcache менеджер с TTL |
| `src/movie_tracker/formatters/` | Rich-таблицы, JSON, CSV |
| `src/movie_tracker/models/` | dataclass-модели |

### Особенности реализации

Версия Claude построена на строгой слоистой архитектуре: слой CLI содержит только UI-логику, API-слой отвечает за HTTP и маппинг ответов, хранилище работает независимо. Каждая команда запускает `asyncio.run()` — Typer-приложение остаётся синхронным снаружи, но полностью async внутри.

HTTP-клиент строит URL явно — `https://kinopoiskapiunofficial.tech/api/v2.2/films` — без base_url, что исключает неочевидное поведение httpx при путях со слешем.

Кэширование реализовано с дифференцированным TTL: поиск — 60 минут, детали фильма — 24 часа, популярное — 3 часа, тренды — 30 минут, рекомендации — 12 часов.

Команды `popular` и `trending` поддерживают автофetch пагинации: при указании `--limit 100` клиент сам загрузит нужное число страниц без необходимости указывать номер страницы вручную.

### Тесты

В версии Claude **85 тестов**. Они покрывают:

- модели данных (SearchResult, WatchlistEntry и другие);
- валидаторы (оценки, теги, год, страница, приоритет);
- операции хранилища (TinyDB CRUD);
- все CLI-команды через CliRunner.

### Итог по Claude Edition

Версия Claude отличается высоким тестовым покрытием (>80%) и чёткой архитектурой. Разработка потребовала большого числа итераций: Claude уточнял детали реализации, исправлял несовместимости библиотек и адаптировал структуру под особенности macOS. Код хорошо разделён по слоям и легко поддаётся сопровождению.

---

## 6. Реализация Codex Edition

Версия `Codex` реализует CLI-приложение с упором на полноту набора команд, детальное разделение по модулям и готовность к распространению через PyInstaller-сборку и GitHub Actions CI/CD.

### Используемые источники данных

- **Kinopoisk API Unofficial** — поиск, детали, популярное, тренды, рекомендации;
- **Локальное хранилище** — watchlist, оценки, отзывы, теги (TinyDB + JSON).

### Технологический стек

| Компонент | Технология | Версия |
|---|---|---|
| CLI-фреймворк | Typer | 0.12+ |
| HTTP-клиент | httpx (sync) | 0.27+ |
| Вывод в терминал | Rich | 13+ |
| Кэширование | diskcache | 5.6+ |
| Локальная БД | TinyDB | 4.8+ |
| Конфигурация | dynaconf | 3.2+ |
| Логирование | loguru | 0.7+ |
| Сборка | hatchling | 1.25+ |
| Бинарная сборка | PyInstaller | 6.6+ |

### Основные файлы проекта

| Файл / Модуль | Назначение |
|---|---|
| `src/movie_tracker/main.py` | Точка входа, Typer app |
| `src/movie_tracker/cli/` | 10 модулей: search, details, watchlist, auth, popular, trending, recommend, cache, errors, state |
| `src/movie_tracker/api/` | 8 модулей: search, details, auth, popular, trending, recommendations, watchlist, client |
| `src/movie_tracker/storage/` | TinyDB watchlist + хранение токена |
| `src/movie_tracker/cache/` | diskcache менеджер |
| `src/movie_tracker/formatters/` | Rich-таблицы, JSON, CSV |
| `packaging/movie-tracker.spec` | PyInstaller spec для бинарной сборки |
| `.github/workflows/` | CI (lint, tests) и release (PyInstaller бинарь) |

### Особенности реализации

Версия Codex построена с более детальным разделением: каждая команда и каждый API-эндпоинт вынесены в отдельный модуль. HTTP-запросы выполняются синхронно, что упрощает структуру кода и снижает порог входа.

Отдельного внимания заслуживает инфраструктура: GitHub Actions автоматически запускает линтер и тесты при push, а при создании тега вида `v1.0.0` собирает PyInstaller-бинарь для Windows, Linux и macOS. Это позволяет распространять приложение без необходимости устанавливать Python.

Конфигурация API-ключа поддерживает три переменные окружения — `MOVIE_TRACKER_API__KEY`, `KINOPOISK_API_KEY` и `KINOPOISK_UNOFFICIAL_API_KEY` — что делает интеграцию в разные среды удобнее.

### Тесты

В версии Codex **12 тестов**. Они покрывают:

- операции watchlist (добавление, список, оценка, статистика);
- поиск через API (нормализация ответа, приоритет токенов);
- CLI-команды (version, init, auth token, list-watchlist);
- валидаторы (оценки, статус, тип, приоритет).

### Итог по Codex Edition

Версия Codex быстрее пришла к рабочему результату. GPT 5.5 потребовалось меньше итераций: структура проекта была сформирована компактно, а основные решения приняты без длительного уточнения. Готовая инфраструктура для распространения (PyInstaller + CI/CD) делает этот вариант удобным для конечных пользователей. Тестовое покрытие значительно ниже, чем у Claude Edition.

---

## 7. Сравнительный анализ

| Критерий | Claude Edition | Codex Edition |
|---|---|---|
| Используемый ИИ | Claude Sonnet 4.6 | GPT 5.5 |
| Основные команды CLI | Все реализованы | Все реализованы |
| Внешний API | Kinopoisk API Unofficial | Kinopoisk API Unofficial |
| HTTP-клиент | httpx async (anyio) | httpx sync |
| Количество CLI-модулей | 5 | 10 |
| Количество API-модулей | 4 | 8 |
| Количество тестов | 85 | 12 |
| Покрытие тестами | >80% | Базовое |
| Система сборки | setuptools | hatchling |
| Бинарная сборка | Нет | PyInstaller |
| CI/CD | Нет | GitHub Actions |
| Поддерживаемые форматы вывода | table, JSON, CSV | table, JSON, CSV |
| Количество итераций | Больше | Меньше |
| Расход токенов | Ниже | Выше (≈2×) |
| Архитектура | Слоистая, строгая | Модульная, детальная |
| Удобство поддержки кода | Меньше файлов | Больше файлов, каждый компактен |

---

## 8. Визуальные материалы и метрики

Ниже приведены данные по использованию моделей за период работы над проектом. Они позволяют сравнить количество запросов, расход токенов и общую стоимость разработки каждой версии.

![График дневных расходов по моделям](assets/usage_metrics/daily_cost.png)

**Рисунок 1.** Дневной график расходов за период разработки.

![Экран usage с токенами и стоимостью](assets/usage_metrics/usage_table.png)

**Рисунок 2.** Статистика запросов и токенов по моделям.

По данным usage-статистики, **GPT 5.5 потратил примерно в 2 раза больше токенов**, чем Claude Sonnet 4.6. При этом GPT 5.5 потребовал **меньше итераций** для достижения рабочего результата: модель формировала структуру проекта и принимала архитектурные решения компактнее, с меньшим числом промежуточных уточнений.

Суммарный расход токенов:

| Модель | Итераций | Токены | Доля токенов |
|---|---:|---:|---:|
| `claude-sonnet-4-6` | Больше | ~ 1× | ~33% |
| `gpt-5.5` | Меньше | ~ 2× | ~67% |

Из этих данных следует, что Claude достиг рабочего результата с меньшим суммарным расходом токенов, хотя и через большее количество шагов. GPT сократил число итераций, но каждая итерация обходилась дороже по токенам — модель генерировала более объёмные ответы с полным кодом за один проход.

---

## 9. Проверка итогового результата

Финальный сценарий работы был проверен для обеих версий.

### Claude Edition

```bash
cd Claude
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
movie-tracker-claude auth token ВАШ_КЛЮЧ
movie-tracker-claude check
movie-tracker-claude search 'Интерстеллар'
movie-tracker-claude details 258687
movie-tracker-claude add-watchlist 258687 --priority 5 --tags 'фантастика,нолан'
movie-tracker-claude list-watchlist --stats
movie-tracker-claude rate 258687 9.5 --review 'Лучший фильм Нолана'
movie-tracker-claude recommend --based-on 258687 --limit 5
movie-tracker-claude trending --window top250 --limit 20
```

### Codex Edition

```bash
cd Codex
python3 -m venv .venv && source .venv/bin/activate
pip install -e .
movie-tracker auth token ВАШ_КЛЮЧ
movie-tracker search 'Интерстеллар'
movie-tracker details 258687 --section all
movie-tracker add-watchlist 258687 --priority 5 --tags 'фантастика,нолан'
movie-tracker list-watchlist --stats
movie-tracker rate 258687 9.5 --review 'Лучший фильм Нолана'
movie-tracker recommend --based-on 258687 --limit 5
movie-tracker trending --type movie
```

Обе версии выполняют основной сценарий: поиск, просмотр деталей, добавление в watchlist, выставление оценки и получение рекомендаций. Данные хранятся локально в `~/.movie-tracker/`.

---

## 10. Особенности сравнения

Обе версии приняли одно и то же ключевое решение — заменить TMDB на Kinopoisk API Unofficial. Это привело к схожим ограничениям: нет OAuth-сессии пользователя, нет облачного watchlist, рекомендации основаны на похожих фильмах Kinopoisk или локальных предпочтениях.

**Claude Sonnet 4.6** сделал ставку на качество: строгую типизацию, async-архитектуру и максимальное тестовое покрытие (85 тестов). Разработка шла итеративно: модель уточняла детали, исправляла проблемы совместимости библиотек и адаптировала код под конкретную среду. Это потребовало большего числа шагов, но каждый шаг был компактным по объёму.

**GPT 5.5** сделал ставку на полноту и инфраструктуру: детальное разделение по модулям, PyInstaller-сборка, GitHub Actions CI/CD. Модель выдавала более крупные блоки кода за один проход, что сократило число итераций, но увеличило расход токенов примерно вдвое. Тестовое покрытие при этом осталось базовым.

Ключевое различие — не в наборе команд (он практически идентичен), а в расстановке приоритетов: Claude сосредоточился на надёжности и тестируемости, GPT — на полноте кода за один проход и готовности к деплою.

---

## 11. Итоговый вывод

Обе версии проекта успешно реализуют требуемую CLI-утилиту. Они соответствуют основной постановке задачи: используют внешний REST API, реализуют полный набор команд, хранят данные локально и корректно обрабатывают ошибки.

**Claude Sonnet 4.6** создал более надёжную с точки зрения тестирования версию: 85 тестов против 12, строгая типизация, чёткое разделение на слои. Меньший расход токенов достигается за счёт итеративного подхода — коротких шагов с уточнениями. Эта версия лучше подходит для долгосрочной поддержки и развития.

**GPT 5.5** создал более готовую к распространению версию: PyInstaller-бинарь, GitHub Actions CI/CD, поддержка трёх форматов переменных окружения. Меньшее число итераций достигается за счёт крупных ответов, что объясняет двукратно больший расход токенов. Эта версия лучше подходит для быстрого релиза и доступности для конечных пользователей без Python.

Главный вывод: оба инструмента эффективны, но по-разному. **Claude** экономичнее по токенам и надёжнее по коду. **GPT** быстрее по числу итераций и лучше готов к деплою из коробки. Выбор зависит от приоритета: если важна надёжность и сопровождаемость — Claude, если важна скорость получения готового к распространению продукта — GPT.